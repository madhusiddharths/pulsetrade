# ─────────────────────────────────────────────────────────────────────────────
# API (FastAPI + co-located MCP subprocess, Option A). Deployment + Service.
# Env comes from TWO sources:
#   - envFrom configMapRef  → non-secret wiring (DB host, API URL)
#   - envFrom secretRef     → API keys (Gemini, Databricks, Tavily) from .env
# This is the K8s equivalent of compose's env_file + environment overrides.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: {{ .Values.replicas.api }}
  # Recreate, not RollingUpdate: the 2×e2-small demo nodes can't host old+new
  # api pods side by side (256Mi request each), so a rolling upgrade deadlocks
  # with the new pod Pending. Brief downtime per upgrade is fine for this demo.
  strategy:
    type: Recreate
    # explicit null: server-side apply otherwise keeps the old rollingUpdate
    # params on the live object, which are forbidden with Recreate
    rollingUpdate: null
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: {{ .Values.images.api }}
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: pulsetrade-config
            - secretRef:
                name: {{ .Values.secretName }}
          resources:
            {{- toYaml .Values.resources.api | nindent 12 }}
          # Liveness/readiness use the endpoints from Day 4. /ready gates on
          # Postgres ONLY (Databricks is reported but not gating, time-boxed at
          # 3s — see api/main.py) so a warehouse outage can't hold the pod out
          # of the Service.
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60      # give the app real time to settle before probing
            periodSeconds: 30
            timeoutSeconds: 15           # was default 1s — the cause of "context deadline exceeded"
            failureThreshold: 10          # tolerate transient slowness before killing
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 10           # /ready hits Databricks — needs a generous timeout
            failureThreshold: 12         # tolerate the ~5min Databricks cold-start
---
# Service — stable DNS name `api`, port 8000. ClusterIP: internal-only for now.
# (Day 13 adds Ingress to expose it publicly; today we reach it via port-forward.)
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
